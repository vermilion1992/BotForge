import ProFormElements from "@/components/ProFormElements";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Pro Form Elements",
};

export default function Page() {
  return <ProFormElements />;
}
