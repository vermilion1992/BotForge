"use client";
import React from "react";

// Minimal stub to unblock compilation
export default function EntryConditionsEditor({ rules, onChange }: { rules: any[]; onChange: (r:any[])=>void }) {
  return (
    <div className="space-y-2">
      <div className="text-sm text-gray-500">Entry Conditions Editor - Coming Soon</div>
      <div className="text-xs text-gray-400">This will be replaced with the full implementation</div>
    </div>
  );
}
